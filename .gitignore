*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# graphify knowledge graph output
/graphify-out/

# build artifacts / local tool caches — not source
/app/release/
/vendor/

# release signing — never commit
/keystore/
keystore.properties

# Firebase config — contains a Google API key; provide locally or via CI secret
# (see fastlane/README.md), never commit
/app/google-services.json

# generated Play Store listing assets — not source
/play-assets/
